#include <iostream>
#include <string>

using namespace std;
int main() {
    float ancho;
    float alto;
    float area;
    cout << "ingrese el alto " << endl;
    cin >> alto;
    cout << "ingrese el ancho" << endl;
    cin >> ancho;
    area = alto * ancho;
}
